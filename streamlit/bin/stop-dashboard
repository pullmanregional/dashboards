#!/bin/bash
if [ $# -lt 1 ]; then
    echo "Usage: $0 <dashboard_name>"
    exit 1
fi

DASHBOARD_NAME=$1
cd "$DASHBOARD_NAME" || { echo "Dashboard directory not found: $DASHBOARD_NAME"; exit 1; }
kill $(cat streamlit.pid) && echo "Dashboard stopped with PID $(cat streamlit.pid)" && rm streamlit.pid